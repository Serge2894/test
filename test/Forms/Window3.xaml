<Window x:Class="test.Forms.Window3"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:test.Forms"
        mc:Ignorable="d"
        Height="500" Width="900" MinWidth="900" MinHeight="500"
        Background="#F5F5F5"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        Topmost="True">

    <Window.Resources>
        <!-- OK Button Style - Green on hover/press -->
        <Style x:Key="OKButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="25"/>
            <Setter Property="Width" Value="70"/>
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4CAF50"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#45A049"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Cancel Button Style - Red on hover/press -->
        <Style x:Key="CancelButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="25"/>
            <Setter Property="Width" Value="70"/>
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#f44336"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#d32f2f"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <Border Grid.Row="0" Background="#DDD" Padding="5" MouseDown="Border_MouseDown">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Room Parameter to Elements" FontWeight="Bold" FontSize="14" VerticalAlignment="Center" HorizontalAlignment="Left" />
                <Button Grid.Column="1" Content="X" Width="25" Height="25" Padding="0"
                        FontWeight="Bold" FontSize="12" Background="Transparent" BorderThickness="0"
                        HorizontalAlignment="Right" VerticalAlignment="Center" Click="btnClose_Click"/>
            </Grid>
        </Border>

        <!-- Content Area - Horizontal Layout -->
        <Border Grid.Row="1" Background="#FFFFFF" CornerRadius="5" Margin="15,10,15,10">
            <Grid Margin="15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" MinWidth="250"/>
                    <ColumnDefinition Width="1*" MinWidth="250"/>
                    <ColumnDefinition Width="1*" MinWidth="250"/>
                </Grid.ColumnDefinitions>

                <!-- Source Parameter Section -->
                <Grid Grid.Column="0" Margin="0,0,10,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Label Content="Source Parameter (from Room):" Grid.Row="0" FontWeight="Bold" FontSize="12"/>

                    <TextBox x:Name="txtSourceParameterSearch" 
                             Grid.Row="1"
                             Margin="0,5,0,5"
                             Padding="5,3"                     
                             TextChanged="txtSourceParameterSearch_TextChanged"
                             GotFocus="txtSourceParameterSearch_GotFocus"
                             LostFocus="txtSourceParameterSearch_LostFocus"
                             Text="Search parameters...">
                        <TextBox.Resources>
                            <Style TargetType="{x:Type TextBox}">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type TextBox}">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="#CCCCCC"
                                                    BorderThickness="1"
                                                    CornerRadius="5">
                                                <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TextBox.Resources>
                    </TextBox>

                    <Border Grid.Row="2" 
                            BorderThickness="1" 
                            BorderBrush="#CCCCCC" 
                            Background="White"
                            CornerRadius="5"
                            Margin="0,5,0,0">
                        <ListView x:Name="lvSourceParameters" 
                                  Background="Transparent"
                                  BorderThickness="0"
                                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                  SelectionMode="Single">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" 
                                                 Content="{Binding ParameterName}"
                                                 Margin="2"
                                                 Checked="SourceParameterCheckBox_Changed"
                                                 Unchecked="SourceParameterCheckBox_Changed"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Border>
                </Grid>

                <!-- Category Selection Section (moved to middle) -->
                <Grid Grid.Column="1" Margin="10,0,10,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Label Content="Select Categories(Inside Room):" Grid.Row="0" FontWeight="Bold" FontSize="12"/>

                    <TextBox x:Name="txtCategorySearch" 
                             Grid.Row="1"
                             Margin="0,5,0,5"
                             Padding="5,3"                     
                             TextChanged="txtCategorySearch_TextChanged"
                             GotFocus="txtCategorySearch_GotFocus"
                             LostFocus="txtCategorySearch_LostFocus"
                             Text="Search categories...">
                        <TextBox.Resources>
                            <Style TargetType="{x:Type TextBox}">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type TextBox}">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="#CCCCCC"
                                                    BorderThickness="1"
                                                    CornerRadius="5">
                                                <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TextBox.Resources>
                    </TextBox>

                    <Border Grid.Row="2" 
                            BorderThickness="1" 
                            BorderBrush="#CCCCCC" 
                            Background="White"
                            CornerRadius="5"
                            Margin="0,5,0,0">
                        <ListView x:Name="lvCategories" 
                                  Background="Transparent"
                                  BorderThickness="0"
                                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                  SelectionMode="Extended">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" 
                                                 Content="{Binding CategoryName}"
                                                 Margin="2"
                                                 FontWeight="{Binding FontWeight}"
                                                 Foreground="{Binding TextColor}"
                                                 Checked="CategoryCheckBox_Changed"
                                                 Unchecked="CategoryCheckBox_Changed"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Border>
                </Grid>

                <!-- Target Parameter Section (moved to right) -->
                <Grid Grid.Column="2" Margin="10,0,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Label Content="Target Parameter:" Grid.Row="0" FontWeight="Bold" FontSize="12"/>

                    <TextBox x:Name="txtTargetParameterSearch" 
                             Grid.Row="1"
                             Margin="0,5,0,5"
                             Padding="5,3"                     
                             TextChanged="txtTargetParameterSearch_TextChanged"
                             GotFocus="txtTargetParameterSearch_GotFocus"
                             LostFocus="txtTargetParameterSearch_LostFocus"
                             Text="Search parameters...">
                        <TextBox.Resources>
                            <Style TargetType="{x:Type TextBox}">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type TextBox}">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="#CCCCCC"
                                                    BorderThickness="1"
                                                    CornerRadius="5">
                                                <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TextBox.Resources>
                    </TextBox>

                    <Border Grid.Row="2" 
                            BorderThickness="1" 
                            BorderBrush="#CCCCCC" 
                            Background="White"
                            CornerRadius="5"
                            Margin="0,5,0,0">
                        <ListView x:Name="lvTargetParameters" 
                                  Background="Transparent"
                                  BorderThickness="0"
                                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                  SelectionMode="Single">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" 
                                                 Content="{Binding ParameterName}"
                                                 Margin="2"
                                                 Checked="TargetParameterCheckBox_Changed"
                                                 Unchecked="TargetParameterCheckBox_Changed"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Border>
                </Grid>
            </Grid>
        </Border>

        <!-- Button Area -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,15,15">
            <Button x:Name="btnOK"
                    Content="OK"
                    Margin="0,0,10,0"
                    Style="{StaticResource OKButtonStyle}"
                    Click="btnOK_Click"/>
            <Button x:Name="btnCancel"
                    Content="Cancel"
                    Style="{StaticResource CancelButtonStyle}"
                    Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>